global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'token-pilot-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # demo profile의 Spring Boot 앱은 호스트에서 실행하고,
      # Prometheus는 host.docker.internal을 통해 scrape합니다.
      - targets: ['host.docker.internal:8080']
